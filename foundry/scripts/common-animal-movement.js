/**
 * Common Animal movement integration.
 *
 * This placeholder exists only so the red Vitest contract can import the
 * future runtime module. The implementation is added in the green step.
 */
export const resolveCommonAnimalMovementRate =
  undefined;

export const synchronizeCommonAnimalTokenMovement =
  undefined;

export const onUpdateCommonAnimalMovementToken =
  undefined;
